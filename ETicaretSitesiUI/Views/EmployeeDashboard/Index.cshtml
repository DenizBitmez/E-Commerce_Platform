
@{
    ViewData["Title"] = "Index";
}

<h2>Hoş Geldiniz, @User.Identity.Name!</h2>

<p>Çalışan Dashboard'una erişiminiz var. Aşağıdaki bağlantıları kullanarak siparişleri ve kategorileri görüntüleyebilirsiniz.</p>

<div class="dashboard-menu">
    <a asp-action="Orders">Siparişleri Görüntüle</a>
    <a asp-action="Categories">Kategorileri Görüntüle</a>
</div>


<style>
    /* Genel stil */
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f8f9fa;
        color: #343a40;
        padding: 20px;
        margin: 0;
    }

    h2 {
        font-weight: 700;
        color: #007bff;
        margin-bottom: 20px;
    }

    p {
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 20px;
    }

    /* Menü stili */
    .dashboard-menu {
        display: flex;
        justify-content: flex-start;
        gap: 15px; /* Butonlar arası boşluk */
        margin-bottom: 20px;
    }

        .dashboard-menu a {
            display: inline-block;
            padding: 12px 24px;
            font-size: 16px;
            font-weight: 500;
            text-decoration: none;
            color: #fff;
            background-color: #007bff;
            border-radius: 4px;
            transition: all 0.3s ease;
        }

            .dashboard-menu a:hover {
                background-color: #0056b3;
                transform: translateY(-2px);
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }
</style>